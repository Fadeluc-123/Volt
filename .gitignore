# Claude Code per-session hook state and personal overrides
.claude/
KB/

# Inbox for handoff bundles awaiting ingestion into KB/
Handoffs/

# Third-party source snapshots (Blink, Zap) kept locally for design reference; re-fetch from GitHub
Reference/

# Roblox definitions luau-lsp analyzes the runtime against; lune run tools/analyze fetches them
Definitions/

# Wally packages, the luau-lsp sourcemap and built place files
Packages/
sourcemap.json
*.rbxl*

# Build products: the bundled compiler with its binaries, and the modules the smoke place is built from
build/
place/Client.luau
place/Server.luau
