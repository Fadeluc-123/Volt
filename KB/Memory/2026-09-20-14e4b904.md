---
type: memory
date: 2026-09-20
session: 14e4b904
description: Ingested the Documents handoff bundle and Mike's code style into the KB (58 new nodes, two new Paths), set up the repo files, and staged everything for Mike's first commit.
tags: [kb, ingestion, setup, code-style]
---
# Documents bundle and code style ingested into the KB; repo staged
Up: [[Memory]]

**What:**
- Split `Documents/` (Zap vs Blink research bundle, session context, handoff, Mike's 16-part code style) into 58 nodes: 16 `code-style-*` plus 2 process nodes in Standards, 7 Decisions, 13 Research, 2 Planning, 7 verbatim Library snapshots, 1 Mistake.
- Added two Paths: **Decisions** (injected in full; added to `$RequiredPaths` in `kb-common.ps1`) and **Library** (listed only, for long verbatim documents). Updated Core, CLAUDE.md and the read-before-work standard to match.
- Kept one copy of the 176 vendored Blink/Zap source files at `Reference/sources/`, described by [[Research/reference-source-snapshots]]; Mike then ruled the folder gitignored and unstaged (local only). Deleted `Documents/` (three duplicate copies plus 8 MB of raw transcripts, whose user messages were all already in the session context).
- Repo: rewrote the UTF-16 README as UTF-8, fixed `.gitignore` (`Handoffs\` was a backslash), added `.gitattributes` (LF), staged 77 files with `git add -A` (Reference/ excluded). No commit; the repo already had `main` and the `Fadeluc-123/Volt` remote.
- New standard [[Standards/address-mike-in-every-message]] (context-rot canary) and [[Standards/working-with-mike]]; auto-memory updated (KB = Knowledge Base Obsidian vault; git 2.55 now present; Mike profile).
**Why:** Mike asked for the Documents context broken down into the KB, code style under Standards, the "address me as Mike" rule, and a git repo.
**How:** Code style nodes keep every rule, example and LEGACY item, drop repo archaeology, and keep section numbers. Research nodes are distilled and point at Library snapshots for citations. Library copies were made with a shell script (verbatim bodies behind frontmatter and a snapshot notice).
**Outcome:** Indexes regenerated and verified; every wiki-link resolves. Injection was about 200 KB; Mike then ruled Research index-only (removed from `$RequiredPaths`), and a hook dry run measured 144 KB with Standards (111 KB) as the bulk. SessionStart hook verified by dry run only, not yet on a real startup with the new Path list.
**Open:** Next session starts [[Planning/volt-next-session-deliverables]]. Confirm goal 10's reading and Lune vs Lute with Mike. Verify the name Volt ([[Decisions/project-name-volt]]).
Related: [[Mistakes/shell-heredoc-tripped-git-guard]] [[Decisions/build-from-scratch-not-fork]] [[Standards/code-style-overview]]
