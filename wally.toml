[package]
name = "fadeluc-123/volt"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
license = "MIT"
description = "Buffer networking for Roblox: a schema compiler and a small runtime"
exclude = ["src/Compiler", "CLAUDE.md", "rokit.toml", "selene.toml", "stylua.toml", "tests", "tools", "place", "build", "KB", "Reference", "Handoffs", "Definitions", "benchmark"]

[dependencies]
